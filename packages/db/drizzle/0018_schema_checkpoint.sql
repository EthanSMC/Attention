-- Schema checkpoint only.
--
-- Migrations 0014-0017 are intentional custom SQL migrations. This no-op
-- entry pairs their resulting schema with meta/0018_snapshot.json so future
-- drizzle-kit generation starts from the deployed schema instead of emitting
-- duplicate DDL for those custom migrations.
SELECT 1;
