{
  "name": "@attention/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "esbuild src/seed-demo-filter.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/seed-demo-filter.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@attention/contracts": "workspace:*",
    "@attention/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
